import 'package:flutter/material.dart';

class ScreenMonthly extends StatelessWidget {
  const ScreenMonthly({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Monthly")),
    );
  }
}
