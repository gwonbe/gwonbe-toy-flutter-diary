import 'package:flutter/material.dart';

class ScreenYearly extends StatelessWidget {
  const ScreenYearly({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Yearly")),
    );
  }
}
