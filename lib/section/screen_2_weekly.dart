import 'package:flutter/material.dart';

class ScreenWeekly extends StatelessWidget {
  const ScreenWeekly({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Weekly")),
    );
  }
}
